<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.androidcompanion">

    <!-- TODO check for a compatible Google Play services APK before access Google Play services features -->
    <!-- TODO verify the version available before attempting to perform API transactions -->
    <!-- automatically install the barcode detection libraries on the device that runs the app -->
    <meta-data android:name="com.google.android.gms.vision.DEPENDENCIES" android:value="barcode"/>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="androidcompanion.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="androidcompanion.device.DeviceListingActivity"
            android:label="@string/app_name"
            android:parentActivityName="androidcompanion.main.MainActivity" />
        <activity android:name="androidcompanion.device.addDeviceToListActivity" />

        <service
            android:name="androidcompanion.notifications.NotificationService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity android:name="androidcompanion.device.ReadQRCodeActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>